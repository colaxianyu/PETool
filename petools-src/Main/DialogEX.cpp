module;

#include <windows.h>
#include <iostream>

module DialogEX;
import STL;

using std::wstring;



//template <typename Derived>
//LRESULT DialogEX<Derived>::dialog_proc(HWND h_dlg, UINT message, WPARAM w_param, LPARAM l_param) {
//    return static_cast<Derived*>(this)->dialog_proc_impl(h_dlg, message, w_param, l_param);
//}